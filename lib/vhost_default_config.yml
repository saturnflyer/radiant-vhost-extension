production:
  top_level_domain: kuviat.com
  redirect_to_primary_site: false
  wildcard_session: true
test:
  top_level_domain: test.com
  redirect_to_primary_site: false
  wildcard_session: false
development:
  top_level_domain: development.com
  redirect_to_primary_site: false
  wildcard_session: false

models:
 Layout:
   name: 
     message:
       'name already in use'
     scope:
       site_id
 Page:
   slug:
     scope: 
       - parent_id
       - site_id
     message:
       'slug already in use for child of parent'
 Snippet:
   name:
     message:
       'name already in use'
     scope:
       site_id
 Asset:
   id:
     message:
       'id already in use'
     scope:
       site_id
